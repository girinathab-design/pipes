import React from "react";
import ReactDOM from "react-dom/client";
import PipesStore from "./PipesStore";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PipesStore />
  </React.StrictMode>
);
